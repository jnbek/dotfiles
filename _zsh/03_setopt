setopt PROMPT_SUBST \
    EXTENDED_GLOB \
    AUTO_CD \
    CORRECT \
    CORRECT_ALL \
    ZLE \
    INTERACTIVE_COMMENTS \
    HIST_IGNORE_DUPS \
    PUSHD_IGNORE_DUPS \
    NOTIFY \
    NOMATCH \
    NOGLOBALRCS
unsetopt NULL_GLOB
[[ $ZSH_VERSION > 4.2.99 ]] && setopt PROMPT_SP    
